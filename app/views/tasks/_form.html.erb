<%= form_with(model: @task, local: true ,url: choose_new_or_edit ) do |form| %>
<%# <%= form_with(model: @task, local: true ,url: tasks_path ) do |form| %><!--投稿画面単体テスト用-->
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= @task.errors.count %>件のエラーがあります。</h2>
      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="task">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
  <%= link_to "task一覧画面にもどる", tasks_path %>
<% end %>
